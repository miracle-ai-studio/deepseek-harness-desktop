# Bundle source

The single canonical external-bundle patch is [`../packages/cordis-plugin/cordis.patch.yml`](../packages/cordis-plugin/cordis.patch.yml). The plugin package references that file from the `patch` field of its `dsh.bundle` manifest; this directory intentionally contains no second package or copied patch.
